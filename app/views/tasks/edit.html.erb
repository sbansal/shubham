<%= content_for(:title, "Tracely - Edit Task for #{current_user.fullname}" )%>
<div class="form_container">
	<h1 class="form_header">Edit the task</h1>
	<div class="seperator"></div>
	<%= render 'form' %>
</div>
<%= render :partial => "common/basket_hint" %>