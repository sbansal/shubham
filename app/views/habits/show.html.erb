<%= content_for(:title, "Tracely - Preview Habit for #{current_user.fullname}" )%>

<p>
  <b>Title:</b>
  <%= @habit.title %>
</p>

<p>
  <b>User:</b>
  <%= @habit.user_id %>
</p>


<%= link_to 'Edit', edit_habit_path(@habit) %> |
<%= link_to 'Back', habits_path %>
